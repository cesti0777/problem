package problem.dfsbfs;

import java.util.Scanner;

public class P1697 {
	static boolean[] map = new boolean[1000001];
	static boolean[] visited = new boolean[1000001];
	static int n;
	static int k;
	static int second;

	public static void main(String args[]) {
		Scanner scanner = new Scanner(System.in);

		n = scanner.nextInt();
		k = scanner.nextInt();
		
		dfs(n, 0);
	}

	public static void dfs(int cur, int sec) {
		visited[cur] = true;
		if(cur == k){
			System.out.println(sec);
			return;
		}
		
		if(cur + 1 < 1000001 && !visited[cur + 1]){
			dfs(cur + 1, sec + 1);
		}else if(cur - 1 > 0 && !visited[cur - 1]){
			dfs(cur - 1, sec + 1);
		}else if(cur * 1 < 1000001 && !visited[cur * 2]){
			dfs(cur * 1, sec + 1);
		}
	}
}
