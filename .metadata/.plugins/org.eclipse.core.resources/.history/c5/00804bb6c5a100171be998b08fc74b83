package samsung.practice1;

import java.util.Scanner;

public class P13460 {
	static char[][] map = new char[101][101];
	static int n = 0;
	static int m = 0;

	public static void main(String[] args) {
		Scanner in = new Scanner(System.in);
		n = in.nextInt();
		m = in.nextInt();
		int cnt = 1;
		while (in.hasNextLine() && in.next() != "\n") {
			String line = in.nextLine();
			for (int j = 1; j <= m; j++) {
				map[cnt++][j] = line.charAt(j - 1);
			}
		}

		for (int i = 1; i <= n; i++) {
			for (int j = 1; j <= m; j++) {
				System.out.print(map[i][j]);
			}
			System.out.println();
		}
	}
}
